#include <bits/stdc++.h>
using namespace std;

// H humans, S seals
// For every human, that human can adopt some seals (can overlap)
// one human can only adopt one seal from the available list
// one seal can only have one owner
// maximum number of seals that can be adopted?

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    
    return 0;
}