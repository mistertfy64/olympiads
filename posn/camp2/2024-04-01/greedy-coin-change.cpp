#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int N, K, ans = 0;
    cin >> N;
    cin >> K;
    vector<int> coins(K);
    for (int i =0 ; i < K; i++){
        cin >> coins[i];
    }
    while (N>0){
        // blah blah blah
    }
    return 0;
}